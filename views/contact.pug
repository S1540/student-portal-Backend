doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Contact Us - Student Portal
    style.
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: "Poppins", sans-serif;
        background: linear-gradient(135deg, #d9f1ff, #ffffff);
        padding: 40px 20px;
        color: #222;
      }
      header {
        text-align: center;
        margin-bottom: 30px;
      }
      h1 {
        font-size: 38px;
        font-weight: 600;
        color: #007bff;
      }
      marquee {
        background-color: #007bff;
        color: #fff;
        padding: 10px 0;
        border-radius: 8px;
        margin: 20px 0 40px 0;
        font-weight: 500;
        font-size: 16px;
        letter-spacing: 1px;
      }
      .contact-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 25px;
        max-width: 1000px;
        margin: 0 auto;
      }
      .contact-info, .contact-form {
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        padding: 30px 25px;
        animation: fadeIn 0.8s ease;
      }
      .contact-info h2, .contact-form h2 {
        color: #007bff;
        font-size: 22px;
        margin-bottom: 15px;
      }
      .info-item {
        margin-bottom: 20px;
      }
      .info-item strong {
        display: block;
        font-weight: 600;
        color: #333;
      }
      .info-item span {
        color: #555;
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 15px;
      }
      input, textarea {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid #ccc;
        border-radius: 10px;
        font-size: 15px;
        transition: 0.3s;
      }
      input:focus, textarea:focus {
        border-color: #007bff;
        outline: none;
        box-shadow: 0 0 5px rgba(0,123,255,0.3);
      }
      textarea {
        resize: none;
        height: 100px;
      }
      button {
        background-color: #007bff;
        color: #fff;
        border: none;
        padding: 12px 20px;
        border-radius: 25px;
        font-size: 15px;
        cursor: pointer;
        transition: 0.3s;
        font-weight: 500;
      }
      button:hover {
        background-color: #0056b3;
        transform: translateY(-2px);
      }
      a.back {
        display: block;
        text-align: center;
        margin: 40px auto 0;
        width: fit-content;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        padding: 12px 30px;
        border-radius: 30px;
        transition: 0.3s;
        font-weight: 500;
        box-shadow: 0 4px 10px rgba(0,123,255,0.3);
      }
      a.back:hover {
        background-color: #0056b3;
        transform: translateY(-3px);
      }
      footer {
        text-align: center;
        margin-top: 50px;
        font-size: 14px;
        color: #666;
      }
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }
  body
    header
      h1 Contact Us
    marquee(direction="left" scrollamount="6") 📞 Get in touch with the Student Support Team | We're here to help you 24/7 | Student Portal 2025
    .contact-container
      .contact-info
        h2 Contact Information
        .info-item
          strong 📍 Address:
          span Student Portal Office Noida,Uttar Pradesh , India
        .info-item
          strong 📧 Email:
          span support@xyz.com
        .info-item
          strong ☎️ Phone:
          span +91 123-456-7890
      .contact-form
        h2 Send a Message
        form(action="/submit" method="POST")
          input(type="text" name="name" placeholder="Your Name" required)
          input(type="email" name="email" placeholder="Your Email" required)
          textarea(name="message" placeholder="Your Message..." required)
          button(type="submit") Send Message
    a.back(href="/") ← Back to Home
    footer
      p © 2025 Student Portal. All rights reserved.
