doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Student Details - National Student Education Portal
    script(src="https://cdn.tailwindcss.com")

  body(class="bg-gray-50 text-gray-800 min-h-screen flex flex-col font-sans")

    // 🔸 Top Bar
    div(class="bg-[#FF9933] text-white text-center py-2 text-xs sm:text-sm font-semibold tracking-wide")
      | 🇮🇳 Ministry of Education | Government of India | Helpline: 1800-111-111

    // 🔹 Header Navbar
    header(class="bg-white shadow-md sticky top-0 z-50 border-b-4 border-[#138808]")
      div(class="container mx-auto flex flex-col md:flex-row justify-between items-center px-6 py-3")
        div(class="flex items-center space-x-2")
          img(src="https://i.pinimg.com/1200x/e2/a0/c9/e2a0c97029dfefe1f7b376f3cba9cc18.jpg" alt="National Emblem" class="h-12 w-12")
          img(src="https://www.nicepng.com/png/detail/80-804165_indian-flag-satyamev-jayate.png" alt="Satyamev Jayate" class="h-12 w-12")
          h1(class="text-xl md:text-2xl font-bold text-[#000080] uppercase ml-2") National Student Education Portal

        nav(class="hidden md:flex gap-8 text-[#000080] font-medium")
          a(href="/" class="hover:text-[#FF9933] transition") Home
          a(href="/about" class="hover:text-[#FF9933] transition") About
          a(href="/students" class="hover:text-[#FF9933] transition") Students
          a(href="/contact" class="hover:text-[#FF9933] transition") Contact

    // 🔹 Marquee
    marquee(direction="left" scrollamount="6" class="bg-[#138808] text-white py-2 font-semibold shadow-inner")
      | 📚 Student Information Portal | View Full Profile & Academic Details | Stay Connected for Updates!

    // 🔹 Main Section
    main(class="flex-grow container mx-auto text-center py-12 px-4")
      h2(class="text-3xl md:text-4xl font-bold text-[#000080] mb-8") Student Details

      // 🔹 Card Section
      div(class="bg-white max-w-7xl w-full mx-auto p-8 rounded-2xl shadow-xl border-t-4 border-[#FF9933] animate-fadeIn")
        // image + details
        div(class="flex flex-col md:flex-row md:items-start md:space-x-10 space-y-6 md:space-y-0")
          
          // Left: Profile Image
          if student.profile
            div(class="flex justify-center md:justify-start")
              img(
                src=student.profile 
                alt="Student Profile" 
                class="h-44 w-44 rounded-xl object-cover shadow-lg"
              )
          
          // Right: Student Details
          div(class="flex-1 grid grid-cols-1 md:grid-cols-3 gap-6")
            p(class="text-lg font-medium text-gray-700")
              | Name : 
              span(class="text-[#000080] font-semibold") #{student.name}

            p(class="text-lg font-medium text-gray-700")
              | Course : 
              span(class="text-[#000080] font-semibold") #{student.course}

            p(class="text-lg font-medium text-gray-700")
              | Age : 
              span(class="text-[#000080] font-semibold") #{student.age}

            if student.email
              p(class="text-lg font-medium text-gray-700")
                | Email : 
                span(class="text-[#000080] font-semibold") #{student.email}

            if student.category
              p(class="text-lg font-medium text-gray-700")
                | category  : 
                span(class="text-[#000080] font-semibold") #{student.category}
            if student.state
              p(class="text-lg font-medium text-gray-700")
                | State  : 
                span(class="text-[#000080] font-semibold") #{student.state}

        //  Back to List Button
        div(class="text-center mt-10")
          a(href="/students" class="inline-block bg-[#000080] hover:bg-[#001860] text-white px-8 py-3 rounded-full font-medium shadow-md transition")
            | ← Back to Students List

    // 🔹 Footer
    footer(class="bg-[#000080] text-white text-center py-6 mt-12")
      div(class="mb-2")
        a(href="#" class="mx-3 hover:text-[#FF9933]") Privacy Policy
        a(href="#" class="mx-3 hover:text-[#FF9933]") Terms of Service
        a(href="#" class="mx-3 hover:text-[#FF9933]") Contact Support
      p(class="text-sm mt-3") © 2025 National Student Education Portal | Ministry of Education, Government of India 🇮🇳

    // 🔸 Animation
    style.
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .animate-fadeIn {
        animation: fadeIn 0.8s ease-in-out;
      }
